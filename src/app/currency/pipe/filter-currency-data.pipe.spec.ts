import { FilterCurrencyDataPipe } from './filter-currency-data.pipe';

describe('FilterCurrencyDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCurrencyDataPipe();
    expect(pipe).toBeTruthy();
  });
});
